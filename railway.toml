[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Let the entrypoint handle gateway startup with token from env
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
